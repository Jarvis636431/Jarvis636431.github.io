# Jarvis Hub Roadmap

这份 Roadmap 用于安排网站从“视觉重构完成”走向“可安全发布、可持续维护”的后续工作。

## 当前状态

- Astro、ESLint、Prettier 和生产构建流程已经建立。
- 首页、博客列表、博客详情、项目列表和项目详情已完成主要视觉重构。
- 博客具备分类筛选、相关文章、目录、正文增强和 Giscus 集成能力。
- 目前 60 篇博客全部为草稿，生产环境不会生成博客详情页。
- 内容目录和 Git 历史中存在疑似真实密码，发布前必须处理。

## P0：发布阻断项

### 1. 清理泄露凭据

- [ ] 确认以下密码是否仍然有效，并立即完成轮换：
  - `idesign1895`
  - `mkDC92Y3`
  - `idesign2019`
- [ ] 将文章中的真实密码替换成明确的占位符。
- [ ] 检查代码、文章、提交历史和构建产物中的其他敏感信息。
- [ ] 使用历史重写工具从 Git 历史中清理已经提交的凭据。
- [ ] 在 CI 中加入敏感信息扫描。

验收标准：

- 当前工作树和完整 Git 历史不再包含有效凭据。
- 被暴露的密码均已失效。
- 新提交中的疑似密码、Token 或私钥会使 CI 失败。

### 2. 建立第一批公开内容

- [ ] 从 60 篇草稿中选择 5–10 篇作为第一批公开文章。
- [ ] 逐篇检查标题、摘要、日期、标签和正文质量。
- [ ] 删除内部地址、账号、密码和不适合公开的信息。
- [ ] 将审核完成的文章设置为 `draft: false`。
- [ ] 验证博客列表、详情页、首页、RSS 和搜索索引均能找到公开文章。

验收标准：

- 生产构建至少生成 5 篇博客详情页。
- RSS、搜索索引和博客分类只包含公开文章。
- 每篇公开文章都通过人工内容审查。

## P1：内容质量与发布流程

### 3. 增加内容检查命令

- [ ] 新增 `pnpm content:check`。
- [ ] 检查重复或为空的 `commentId`。
- [ ] 检查摘要长度、空标签、无效日期和重复标题锚点。
- [ ] 检查疑似密码、Token、内网地址和敏感配置。
- [ ] 校验标签数量，建议每篇文章保持 2–4 个标签。
- [ ] 将内容检查加入 `pnpm verify` 和 GitHub Actions。

验收标准：

- 错误内容会返回非零退出码，并指出具体文件和原因。
- 本地验证和 CI 使用同一套内容规则。

### 4. 规范文章元数据

- [ ] 定义稳定的博客主分类和标签词表。
- [ ] 清理含义重复或颗粒度不一致的标签。
- [ ] 确认迁移文章的真实发布日期。
- [ ] 根据正文自动计算阅读时间。
- [ ] 仅在内容实际更新时填写 `updatedDate`。
- [ ] 评估是否增加 `series` 字段，用于组织系列文章。
- [ ] 评估是否增加 `featured` 字段，用于首页和博客列表精选内容。

验收标准：

- 主分类数量保持稳定。
- 页面分类、相关文章和搜索结果使用相同的内容语义。
- 新建文章脚本能生成符合规范的 frontmatter。

### 5. 建立统一内容查询层

- [ ] 在 `src/lib` 中集中实现公开内容过滤和日期排序。
- [ ] 提供 `getPublishedPosts()`。
- [ ] 提供 `getPublishedProjects()`。
- [ ] 提供 `getRecentPosts()` 和 `getRelatedPosts()`。
- [ ] 移除首页、RSS、搜索和详情页中的重复查询逻辑。

验收标准：

- 草稿与归档过滤规则只有一个实现来源。
- 首页、列表、RSS 和搜索的内容结果保持一致。

## P1：性能与基础体验

### 6. 优化图片资产

- [ ] 压缩当前约 800 KB 的 `public/logo.png`。
- [ ] 为 favicon 提供独立的小尺寸文件或 SVG。
- [ ] 为 Open Graph 提供独立的 1200×630 分享图。
- [ ] 检查项目封面的尺寸、格式和加载策略。
- [ ] 为重要图片补充明确的 `width` 和 `height`，降低布局偏移。

验收标准：

- Logo 不再以 1254×1254 原图承担所有用途。
- 页面图片不会产生明显布局跳动。
- 分享卡片使用正确的尺寸和内容。

### 7. 优化字体加载

- [ ] 统计 IBM Plex Mono、Space Grotesk 和 Newsreader 的实际使用范围。
- [ ] 删除不再需要的字体。
- [ ] 将保留字体本地托管或提供可靠的系统字体降级。
- [ ] 配置字体预加载和合适的 `font-display`。
- [ ] 检查国内网络环境下的加载表现。

验收标准：

- 页面不依赖不可用的外部字体才能正常显示。
- 首次加载时没有明显的字体闪动和布局偏移。

### 8. 统一页面语言与基础元数据

- [ ] 确定全站主语言，当前建议为 `zh-CN`。
- [ ] 修正首页的 `lang="en"`。
- [ ] 统一导航、搜索、404、Footer 和按钮文案。
- [ ] 将 `<meta name="theme-color">` 调整为实际页面背景色。
- [ ] 检查每个页面的标题、描述、canonical 和结构化数据。

验收标准：

- 页面语言声明与主要内容一致。
- 浏览器地址栏主题色与网站视觉一致。
- 核心页面具有正确且唯一的 SEO 元数据。

### 9. 中文化并统一全局搜索

- [ ] 中文化搜索框、空状态、错误状态和索引状态。
- [ ] 将结果类型从 `blog/project` 改为“文章/项目”。
- [ ] 统一搜索弹窗与当前编辑型视觉语言。
- [ ] 检查键盘导航、焦点恢复和移动端输入体验。
- [ ] 为搜索索引请求失败提供安静、清晰的降级状态。

验收标准：

- 搜索全流程不再混用中英文界面文案。
- 鼠标、键盘和移动端均可完成打开、搜索、选择和关闭操作。

## P2：工程维护与可靠性

### 10. 清理未使用代码

- [ ] 确认并删除未使用的组件：
  - `BlogCard.astro`
  - `ProjectCard.astro`
  - `PixelCube.astro`
- [ ] 确认并删除已经完成使命的一次性脚本：
  - `scripts/fix-mdx-brackets.mjs`
  - `scripts/fix-mdx-tags.sh`
- [ ] 清理无效注释、旧设计类名和不再使用的 Tailwind token。
- [ ] 更新 README 中的目录结构和内容管理说明。

验收标准：

- 删除内容没有运行时引用。
- `pnpm verify` 在清理后保持通过。

### 11. 统一路径别名

- [ ] 对齐 `tsconfig.json` 与 Astro/Vite 的路径别名。
- [ ] 确认 `@lib`、`@components`、`@layouts` 等别名均被类型系统识别。
- [ ] 避免同一目录同时使用别名和多层相对路径。

验收标准：

- 编辑器、Astro Check 和生产构建对所有别名解析一致。

### 12. 增加关键交互测试

- [ ] 为博客分类筛选增加测试。
- [ ] 为全局搜索和键盘导航增加测试。
- [ ] 为移动菜单焦点管理增加测试。
- [ ] 为文章目录、重复标题和折叠状态增加测试。
- [ ] 为代码复制、表格滚动和返回顶部增加测试。
- [ ] 验证未配置 Supabase 和 Giscus 时页面正常降级。

验收标准：

- 关键用户路径在桌面和移动视口下均有自动化覆盖。
- 交互回归能在合并前被 CI 捕获。

### 13. 审核浏览量统计

- [ ] 检查 Supabase RPC 权限和 RLS 配置。
- [ ] 明确刷新、机器人和重复访问的计数策略。
- [ ] 增加必要的限流或简单去重。
- [ ] 确认统计失败不会影响首页其他功能。
- [ ] 如果浏览量没有明确产品价值，评估移除该功能及依赖。

验收标准：

- RPC 不能被用于任意数据修改。
- 失败状态不会产生报错界面或阻塞页面。
- 统计规则有简短文档说明。

## P2：首页与内容闭环

### 14. 使用真实内容完善首页

- [ ] 展示最近发布的 3 篇文章。
- [ ] 展示精选项目。
- [ ] 增加“当前关注/正在学习”的轻量内容区。
- [ ] 确保首页入口能自然进入博客和项目详情。
- [ ] 删除没有真实数据支撑的展示模块。

验收标准：

- 首页内容来自 Content Collections，而不是重复硬编码。
- 新文章或项目发布后，首页可以自动更新。

## Backlog：实验性想法

以下内容不作为近期目标。只有在内容、安全、性能和可访问性工作完成后，再根据整体设计方向评估。

- [ ] Astro View Transitions。
- [ ] 最近阅读或学习内容卡片。
- [ ] GitHub 近期活动。
- [ ] Steam 游戏状态与成就。
- [ ] 季节或时间相关的轻量彩蛋。
- [ ] 低强度背景纹理。
- [ ] 受控的卡片倾斜或局部微交互。

暂不建议：

- 全局 Glitch 效果。
- 大范围磁性按钮。
- 点击波纹覆盖所有控件。
- 代码雨或高强度动态背景。

这些效果容易破坏当前已经形成的克制、编辑型和项目档案式视觉语言。

## 建议执行顺序

1. 清理并轮换泄露凭据。
2. 选择并发布第一批文章。
3. 实现内容检查命令并接入 CI。
4. 规范标签、日期、摘要和阅读时间。
5. 优化 Logo、字体和页面元数据。
6. 中文化搜索并统一全站语言。
7. 清理未使用代码和一次性脚本。
8. 抽取统一内容查询层。
9. 增加关键交互测试。
10. 完成首页与真实内容的闭环。
