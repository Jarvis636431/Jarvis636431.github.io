---
title: "小米——大前端"
description: "1. 事件循环输出结果顺序"
commentId: "blog-小米-大前端"
publishDate: "2025-06-01"
tags: [
  "前端", "面经"
]
draft: true
readingTime: 3
---

# 一面
## 手撕
1. 事件循环输出结果顺序
### 八股
1.有没有使用到什么异步的操作
2.< script >标签里的 async 和 defer 有什么区别
### 项目拷打
1.讲讲你觉得做的最好/最有成就感的一个项目
2.我看到你简历里有一个 Three.js的应用，讲讲你都做了什么工作
3.学生工作
# 二面
### 手撕
1. 调用原生模拟模块读取文件：
请使用提供的requireNativeModule函数，调用FileReader模块的readFile方法，读取一个文件路径为'/test.txt'的文件，并将结果显示在控制台。

```javascript
// 模拟的原生模块
const NativeModules = {
  FileReader: {
    readFile: (path, callback) => {
      // 模拟异步读取文件
      setTimeout(() => {
        // 假设文件内容总是"Hello, Bridge!"
        callback(null, `文件内容: Hello, Bridge! from ${path}`);
      }, 100);
    }
  }
};
// 一个包装函数，用于将回调风格的API转换为Promise风格
function requireNativeModule(moduleName) {
  const module = NativeModules[moduleName];
  return {
    readFile: (path) => {
      return new Promise((resolve, reject) => {
        module.readFile(path, (err, data) => {
          if (err) reject(err);
          else resolve(data);
        });
      });
    }
  };
}
// === 使用示例 ===
// 获取 Promise 化后的 FileReader 模块
const FileReader = requireNativeModule("FileReader");
// 调用 readFile 并输出结果
FileReader.readFile("/test.txt")
  .then((content) => {
    console.log("读取成功:", content);
  })
  .catch((err) => {
    console.error("读取失败:", err);
  });

```

2. CodeSandBox 题目：修复两个 Bug，一个是依赖 useEffect 缺失依赖数组，另一个是使用 useMemo 优化更新
3. CodeSandBox 题目：使用 React.Memo 优化组件渲染
### 八股：
1. 讲讲 React.Memo 和 useMemo 有什么区别
2. 为什么要有事件循环/事件循环解决了什么问题
3. 有了事件循环就一定不会卡顿么
4. 为什么要使用状态管理库
5. 选一个状态管理库讲讲思想——Redux
6. RN 和 React 的渲染机制的对比
7. 性能优化实践过哪些
8. 对于 WASM 有什么了解
### 场景题
1. 看到你有很多的组件化业务，那如果让你封装一个组件，应该考虑哪些地方
# 三面 负责人面——12.1
小米研发总负责人面试——服务端出身
如何设计一个拼团系统，表怎么设计
Ai 的影响，会不会替代前端程序员
用过服务端么，学过什么框架
如果要学一门新语言新框架，借助 AI该怎么学习
反问：
人车家生态打通对于将来的影响
智能家居
小模型
AI 智能的发展
机器人发展，实际场景，具身智能
# 四面 HR 面 ——12.3
家庭
经历过最有压力的事，讲讲项目中的实际经历
介绍一下你的兴趣爱好
讲讲你最喜欢的三个球星
你对于 AI 能替代前端程序员这种事怎么看待的
讲讲学业上的困境
为什么放弃保研
当场告知通过
# 五面 总经理面——12.10
你是学什么专业的，主要涉及了哪些课，一般会去哪里工作
讲讲你最有成就感的事
讲讲你的爱好
给我推荐三个游戏，必须有排序然后排序理由
你是怎么学前端的，我看你全栈都做过，为什么选择了前端
你说前端的学习曲线比较平滑，那意味着上限也低，你怎么看待这个事
讲讲你日后的工作规划和人生规划，你想达到一个什么样的高度
如果让你选择你个人出名还是你做的框架出名，你会选择哪个
你有了解过ai 么，用过哪些工具，应用场景有哪些，你怎么看待 AI 会代替前端程序员这个事
反问：业务，为什么五面，可能还有第六面
# 六面 


